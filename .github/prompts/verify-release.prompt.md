---
description: Verify an issue is ready for the simulated release workflow.
agent: qa
---

Use the release-simulation and testing skills. Verify the linked issue has `ready-for-qa`, the pull request is merged, all acceptance criteria have evidence, and the main branch passes API tests, automation tests, frontend lint, and frontend build. Report blockers; do not add `release-to-production` or perform a real deployment.
